gymnasium>=0.29
numpy>=1.26
torch>=2.3
pyyaml>=6.0
tensorboard>=2.15
tbparse>=0.0.7        # export_tb_summary.py - parse TB event files for diagnostic sharing
# Optional:
pynput>=1.7           # --human-override keyboard capture. Install to enable manual bot steering.
# wandb>=0.16
# msgpack>=1.0
# hydra-core>=1.3
